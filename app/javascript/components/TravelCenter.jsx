import React from 'react'

const TravelCenter = (props) => {
    const butt = "butt"
    debugger;
    const ass = "ass"
    return (
        <div>
            {props.travelCenter.name} 
        </div>
    )
}

export default TravelCenter;